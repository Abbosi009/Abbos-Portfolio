Abbos Ismoilov — Portfolio (static)

Files:
- index.html — main page
- styles.css — styles (dark + light)
- script.js — theme toggle + form demo

Quick preview:
Open `index.html` in your browser (double-click or use a local server).

Notes:
- Theme is persisted in `localStorage`.
- Contact icons show only icons visually; hover to see tooltips with contact details.
- The "Send Message" form is client-side only (simulates sending).

Next steps I can help with:
- Add a backend endpoint to actually send messages (Node/PHP/Netlify functions).
- Improve responsive layout and add portfolio sections.
